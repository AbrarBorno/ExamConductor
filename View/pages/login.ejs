<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="" />
    <title>Log In</title>

    <link rel="stylesheet" href="/static/css/bootstrap.min.css" />
    <meta name="theme-color" content="#7952b3" />

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
      html {
        height: 100%;
      }
      body {
        padding-top: 56px;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .sense {
        width: 400px;
        height: 300px;
        background-color: rgb(230, 230, 230);
        border-radius: 8px;
        padding-left: 8px;
        padding-right: 8px;
        padding-top: 30px;
      }
      .sense > p {
          text-align: center;
          font-size: 30px;
          font-weight: bold;
          font-family: "Verdana";
          color: rgb(136, 136, 136);
      }
      form > .inspace {
        padding-top: 6px;
        padding-bottom: 6px;
        margin-left: 6px;
        margin-right: 6px;
      }
      form > button {
        float: right;
        margin-right: 6px;
        margin-top: 8px;
      }
      .ermsg {
        margin-left: 6px;
        margin-right: 6px;
        border: 1px solid rgb(190, 3, 3);
        background-color: rgba(190, 3, 3, 0.212);
        color: rgb(190, 3, 3);
        padding-left: 4px;
        padding-right: 4px;
        border-radius: 3px;
      }
      .signup {
        margin-left: 6px;
        margin-top: 8px;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <div class="container-fluid">
        <span class="navbar-brand">Login</span>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <li class="nav-item">
              <a class="nav-link active hlink" aria-current="page" href="/board">Home</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="sense">
      <p>Login</p>
      <form action="login" method="POST">
        <% if(error) { %>
          <p class="ermsg"><%= errorMsg %> </p>
        <% } %>
        <div class="form-group inspace">
          <input
            type="text"
            class="form-control"
            id="userbox"
            placeholder="Username"
            name="username"
            required
          />
        </div>
        <div class="form-group inspace">
          <input
            type="password"
            class="form-control"
            id="passbox"
            placeholder="Password"
            name="password"
            required
          />
        </div>
        <button type="submit" class="btn btn-primary">Login</button>
        <a href="/signup" class="btn btn-outline-primary signup" role="button">Signup</a>
      </form>
    </div>
    <script src="/static/js/bootstrap.bundle.min.js"></script>
    <script src="/static/js/jquery-3.6.0.min.js"></script>
    <script src="/static/js/auths.js"></script>
  </body>
</html>
