<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="" />
    <title>Info</title>

    <link rel="stylesheet" href="/static/css/bootstrap.min.css" />
    <meta name="theme-color" content="#7952b3" />

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
      html {
        height: 100%;
      }
      body {
        padding-top: 56px;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .sense {
        width: 80%;
        background-color: rgb(230, 230, 230);
        border-radius: 8px;
        padding-left: 30px;
        padding-right: 30px;
        padding-top: 30px;
        padding-bottom: 30px;
      }
      .sense > p {
          text-align: left;
          font-size: 30px;
          font-weight: bold;
          font-family: "Verdana";
          color: rgb(136, 136, 136);
      }
      form > .inspace {
        padding-top: 6px;
        padding-bottom: 6px;
        margin-left: 6px;
        margin-right: 6px;
      }
      form > button {
        float: right;
        margin-right: 6px;
        margin-top: 8px;
      }
      .ermsg {
        margin-left: 6px;
        margin-right: 6px;
        border: 1px solid rgb(190, 3, 3);
        background-color: rgba(190, 3, 3, 0.212);
        color: rgb(190, 3, 3);
        padding-left: 4px;
        padding-right: 4px;
        border-radius: 3px;
      }
      .hlink {
        border: 0;
        border-radius: 5px;
        background-color: #3e4347;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <div class="container-fluid">
        <span class="navbar-brand">Message</span>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <li class="nav-item">
              <a class="nav-link active hlink" aria-current="page" href="/board">Home</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="sense">
    <% if (code === 7) { %>
        <p>ERROR: <%= data %> </p>
    <% } else if (code === 6) { %>
        <p>The exam hasn't started yet. Come again at <b><%= data %></b> to attempt</p>
    <% } else if (code === 1) { %>
        <p>Click the link below to start your exam. You'll get <b><%= data.duration %></b> minutes if you start now</p>
        <a class="btn btn-primary" href="<%= data.link %>">Start Exam</a>
    <% } else if (code === 3) { %>
        <p>You have submitted this exam. Re visit this page on <b><%= data %></b> to view your results</p>
    <% } else if (code === 5) { %>
        <p>The window for this exam has expired. You haven't attempted this exam</p>
    <% } else { %>
        <p>Unknown error occured</p>
    <% } %>
    </div>
    <script src="/static/js/bootstrap.bundle.min.js"></script>
    <script src="/static/js/jquery-3.6.0.min.js"></script>
  </body>
</html>
